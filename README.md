Create a new feature: "Staff Activity Dashboard"

**Requirements:**
1. Create a view that shows:
   - List of all staff in the user's organization
   - Each staff member's total events this month
   - Last login time
   - Status (active/inactive)
2. Add filters:
   - By role (caregiver, alternate, RN, etc.)
   - By status (active/inactive)
   - Date range for events
3. Use crispy forms for filters
4. Use Bootstrap 4 for styling
5. Add sorting (by name, by event count)
6. Add CSV export button

**Deliverables:**
- View code (with proper querysets)
- URL pattern
- Template (using template inheritance)
- Screenshots of working feature
